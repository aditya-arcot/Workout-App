[project]
name = "server"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "alembic[tz]>=1.17.2",
    "fastapi[standard]>=0.124.4",
    "pydantic-settings>=2.12.0",
]

[dependency-groups]
dev = ["pytest>=9.0.2", "ruff>=0.14.9"]
